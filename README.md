## Acceptance testing an Ember.js front-end

* `bundle`
* run the front-end (`npm start` in the repo)
* `rspec`

## Features:

* Rspec is driving Capybara via PhantomJS
* Screenshots are saved upon failure into `./screenshots`